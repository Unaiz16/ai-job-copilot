# Copy to frontend/.env for local development.
# Every VITE_* variable is bundled into the public JavaScript, so never put
# secrets (Gemini, Browserless, Supabase service keys...) here. Those belong
# in backend/.env and are only used server-side.
VITE_API_BASE_URL=https://ai-job-copilot.onrender.com
